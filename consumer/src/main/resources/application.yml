
spring:
  main:
    banner-mode: OFF
  rabbitmq:
    addresses: localhost:5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: auto
        retry:
          enabled: true
          initial-interval: 3s
          max-interval: 10s
          max-attempts: 2
          multiplier: 2
